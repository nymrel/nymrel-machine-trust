# Normalize line endings: LF in the repository and in every checkout.
# Windows checkouts previously got CRLF via core.autocrlf, which broke
# byte-identical specimen hash verification. Binary assets are untouched.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.zip binary
